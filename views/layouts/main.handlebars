<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Thomas' Lemon Chess Blog</title>
  <!-- Link to the Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Link to your custom CSS file -->
  <link rel="stylesheet" type="text/css" href="/css/dashboard.css">
  <!-- Custom styles for the lemon theme -->
  <style>
    body {
      background-color: #FFD700; /* Lemon color */
      color: #333; /* Dark gray text */
    }

    .navbar {
      background-color: #32CD32; /* Lime Green */
    }

    .navbar-dark .navbar-toggler-icon {
      background-color: #333; /* Dark gray color for the toggler icon */
    }
  </style>
</head>
<body>
  <header>
    <nav class="navbar navbar-expand-lg navbar-dark">
      <div class="container-fluid">
        <a class="navbar-brand fs-2" href="/">Thomas' Lemon Chess Blog</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="/">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/dashboard">Dashboard</a>
            </li>
            {{#if logged_in}}
            <li class="nav-item">
              <a class="nav-link" href="#" id="chess-logout">Logout</a>
            </li>
            {{else}}
            <li class="nav-item">
              <a class="nav-link" href="/login">Login</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/signup">Sign Up</a>
            </li>
            {{/if}}
          </ul>
        </div>
      </div>
    </nav>
  </header>
  <div class="container" style="margin-bottom: 15vh;">
    {{{body}}}
  </div>
</body>
</html>


